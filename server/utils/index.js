const setCookie = require("./setCookie");
const generateToken = require("./generateToken");
const authenticate = require("./authenticate");

module.exports = { setCookie, generateToken, authenticate };
